# 组件目录约定

- `common/`：跨页面复用的基础组件（如按钮、全局小组件）。
- `forms/`：表单类交互组件（如 Tab 切换、输入相关）。
- `layout/`：布局与背景类组件（如全局视频背景）。
- `system/`：系统级导航、权限入口等全局组件。

说明：
- 历史组件文件仍保留在 `components/` 根目录，避免一次性大迁移导致引用中断。
- 新代码请优先从上述子目录引入；旧文件可在后续迭代中逐步迁移并删除。
